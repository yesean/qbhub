import { Heading } from '@chakra-ui/react';

const Info: React.FC = () => {
  return (
    <Heading pl={4} size="sm" mb={4}>
      Science / 2019 Chicago Open / Open
    </Heading>
  );
};

export default Info;
